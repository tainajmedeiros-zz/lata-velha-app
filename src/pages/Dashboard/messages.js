import { defineMessages } from 'react-intl';

const prefix = 'components.Dashboard';

export default defineMessages({
  listError: {
    id: `${prefix}.listError`,
    defaultMessage: 'Erro ao carregar a lista',
  },
});
