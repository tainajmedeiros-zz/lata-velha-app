import { defineMessages } from 'react-intl';

const prefix = 'components.NotFound';

export default defineMessages({
  notFoundTitle: {
    id: `${prefix}.notFoundTitle`,
    defaultMessage: 'Página não encontrada!',
  },
});
