import { defineMessages } from 'react-intl';

const prefix = 'components.ActionBar';

export default defineMessages({
  addButton: {
    id: `${prefix}.addButton`,
    defaultMessage: 'Adicionar',
  },
});
