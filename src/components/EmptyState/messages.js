import { defineMessages } from 'react-intl';

const prefix = 'components.EmptyState';

export default defineMessages({
  emptyTitle: {
    id: `${prefix}.emptyTitle`,
    defaultMessage: 'ops, nada para ver aqui...',
  },
});
